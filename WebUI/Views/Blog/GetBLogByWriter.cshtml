@{
    ViewData["Title"] = "GetBLogByWriter";
    Layout = "~/Views/Shared/WriterLayout.cshtml";
}

@using EntityLayer.Concrete
@model List<Blog>

<div class="container">
    <div class="row h1">Your Blogs</div>
    <div class="row">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th> Id </th>
                    <th> Title </th>
                    <th> Date </th>
                    <th> Category </th>
                    <th> Status </th>
                    <th class="text-center"> Operations</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model as List<Blog>)
                {
                    <tr>
    using DocumentFormat.OpenXml.Drawing.Diagrams;
                        <td> @item.Id </td>
                        <td> @item.Title </td>
                        <td> @item.CreatedDate </td>
                        <td>
                            @foreach (var tag in item.Tags?.ToList() as List<Tag>) {
                                @tag
                            }
                        </td>
                        <td> @(String.IsNullOrEmpty(@item.Content) ? "No Content" : @item.Content.Substring(0, 30))... </td>
                        <td>
                            <a class="btn btn-warning btn-sm" href="/blog/blogadd/@item.Id">Update</a>
                            <a class="btn btn-danger btn-sm" href="/blog/blogdelete/@item.Id">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <div class="d-flex justify-content-end">
            <a class="btn btn-info" href="/blog/blogadd">Add</a>
        </div>
    </div>
</div>
